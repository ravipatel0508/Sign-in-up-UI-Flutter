import 'package:flutter/material.dart';

emptyVerticalBox({double height = 10}) => SizedBox(
      height: height,
    );
emptyHorizontalBox({double width = 10}) => SizedBox(
      width: width,
    );
